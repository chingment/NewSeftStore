<view id="vmsimulatedDATA">

  <view class='productConten'>

    <view class="productDelcom" wx:for="{{simulatedDATA.specItems}}" wx:for-index='n' wx:for-item="ProductItem">

      <view class='p'>{{ProductItem.name}}</view>

      <ul class="productFooterlist clearfix">

        <li wx:for="{{ProductItem.value}}" wx:for-item="oItem" bindtap="specificationBtn" data-show="{{oItem.isShow}}" class="{{oItem.isShow?'':'noneActive'}} {{specSubIndex[n] == index?'productActive':''}}" data-ol="{{specSubIndex[n]}}" data-name="{{oItem.name}}" data-n="{{n}}" data-index="{{index}}">{{oItem.name}}</li>

      </ul>

    </view>

  </view>

  <view wx:if="{{specBoxArr.id}}">

    {{specBoxArr.id+'--'+specBoxArr.price}}

  </view>

</view>